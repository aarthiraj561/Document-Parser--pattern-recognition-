<h1>/Passage</h1>

<p>
  <b>Passage</b><br>
  <%= @passage.passage %>
</p>

<div class="form-actions">
  <%= link_to 'Back', passages_path, :class => 'btn'  %>
  <%= link_to 'Edit', edit_passage_path(@passage), :class => 'btn' %>
  <%= link_to 'Delete', passage_path(@passage), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
</div>
